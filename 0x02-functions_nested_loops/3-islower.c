#include "main.h"
/**
 * checks for lower case character
 *
 * return 1 if c is lowercase
 * Returns 0 otherwise
 */
int main (void);
{
	int _islower(int c);
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	return (0);
}
